import 'package:flutter/material.dart';

class NewHistoryInstance extends StatelessWidget {
  const NewHistoryInstance({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
